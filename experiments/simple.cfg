# vim: ft=python
#
p1 = parameters()
p1.max_steps = 2000
p1.max_joint_distortion = 30
p1.max_dead_joint_distortion = 10
p1.max_cells = 400
add_treatment('distort_10', p1, replicates=1)
# p1.max_joint_distortion = 20
# add_treatment('distort_20', p1, replicates=2)

# add_treatment('control', p1, replicates=2)
# add_treatment(name='control', replicates=50)

# add_analysis(series)
# add_analysis(dump)
# add_analysis(sizes)

# grow_step = .1, max_steps = 3000, finish_size=20,
# single_branch_prob=.1, max_cells=1000,
# max_bias=1000.)

# pymode:lint_ignore=W802
